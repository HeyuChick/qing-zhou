# 轻舟 — 开发模式启动
# 前端热更新：QZ_WEB_DIR 让服务直接读磁盘上的 web/dist，
#   改 html/css/js 后浏览器刷新即可，无需重启、无需重新编译。
# 后端：go run 每次启动都会重新编译最新代码。
$env:QZ_LISTEN  = '127.0.0.1:8081'   # 浏览器访问 http://127.0.0.1:8081
$env:QZ_WEB_DIR = 'web/dist'         # 前端热更新开关（生产留空，用内嵌资源）

# SMTP 等配置请在面板「设置」页填写并保存（保存后重启本服务生效），
# 这样密码不会出现在脚本里。

go run .
