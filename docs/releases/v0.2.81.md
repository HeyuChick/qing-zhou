# v0.2.81 — ACME证书兼容性增强、OAuth安全修复与订阅兼容性改进

## 功能增强

### ACME证书兼容性增强
- 改进ACME客户端兼容性，支持更多证书颁发机构
- 增强证书检查模块，提供更详细的证书状态信息
- 优化前端证书管理界面，提升用户体验
- 完善数据库迁移逻辑，确保证书数据一致性
- 添加完整的测试用例，覆盖核心功能

### 订阅兼容性改进
- 默认省略sing-box tun.stack配置，保留1.14兼容路径
- 提升订阅配置的兼容性和稳定性
- 添加相关测试用例验证配置生成

### 前端改进
- 优化证书管理页面的交互体验
- 改进证书状态显示和错误提示

## 安全修复

### OAuth浏览器隔离
- 修复OAuth流程中浏览器cookie未正确隔离的问题
- 确保每个登录流程使用独立的浏览器会话
- 增强OAuth认证的安全性
- 添加相关测试用例验证修复效果

## 技术改进

### 测试覆盖
- 为ACME证书功能添加完整的单元测试
- 为OAuth安全修复添加测试用例
- 为订阅配置生成添加测试用例
- 确保所有变更都经过充分测试

### 代码质量
- 优化证书相关代码结构
- 改进错误处理和日志记录
- 提升代码可维护性

## 验证与运行说明

验证命令：`go test ./...`、`go vet ./...`、前端 `npm test`、`npm run typecheck`、`npm run build`。

本次更新主要关注证书管理、认证安全和订阅兼容性，建议所有用户升级以获得更好的安全性和兼容性。

## 升级说明

1. 备份当前数据库和配置文件
2. 下载新版本二进制文件
3. 重启服务
4. 验证证书管理功能正常工作
5. 测试OAuth登录流程
6. 验证订阅链接生成功能正常工作